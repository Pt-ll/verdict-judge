# 更新日志

## 0.1.3 — 2026-09-18

四条来自真实使用的改进：数据集中放、比赛可以开好几场、题目删得动、成绩单看得到测试点。

- **新**：**测试数据总库**。题目的数据统一放在 `.verdict/data/<题目 id>/`，用文件夹名区分题目；
  题目包（`.verdict/problems/<id>/`）里只剩 `problem.json` 与 `extra/`。新建题目、导入题目包
  都会落到总库；`problem.json` 里测试点路径因此写成相对数据目录的 `1.in`。
  0.1.2 及更早的两种写法都照旧能读：包内 `data/` 仍被认成数据目录，`"data/1.in"`
  加载时归一成同一个意思，老工作区不用搬数据。想放别处也行——`problem.json` 的
  `dataDir` 字段（相对题目包根目录）说了算。
- **新**：**一个工作区可以放多场比赛**。比赛配置从单文件 `.verdict/contest.json` 变成
  `.verdict/contests/<比赛 id>.json`，评测记录同样一场一份（`.verdict/submissions/<id>.json`）。
  题目库是全局共用的，所以同一道题可以同时出现在多场比赛里（各场的分数、重测次数互不干扰）。
  面板「题目」页签顶上多了比赛下拉框与「＋ 再开一场」，命令面板有「Verdict: 切换比赛」。
  旧的 `contest.json` / `submissions.json` 继续读，两种布局可以共存。
- **新**：**删除题目**。面板上每道题都有 🗑，也可以走命令面板的「Verdict: 删除题目」。
  确认框里给三个选择：只从比赛移除 / 删除题目包（保留数据）/ 连数据一起删除。
  删除走 VS Code 的回收站接口，删错了能捞回来；题目同时出现在好几场比赛里时，
  会先从所有比赛里摘掉，免得别的比赛下次加载直接报错。
- **修**：**成绩单里点单元格看不到测试点详情**。单元格上的键是「选手 id × 题目 id」，
  而内嵌详情是按**显示名**建的索引（`alice` 显示成 `Alice`），两边对不上，于是点了没有任何反应。
  现在详情里单独存一个与 `data-cell` 逐字一致的键，并加了一条单测盯着这两者必须相等。
- **新**：**成绩单 HTML 里能看到测试点详情**。导出的榜单现在带一节「题目与测试点」：
  每道题的限制、比较方式、子任务表（分值 / 依赖 / 计分方式 / 包含哪些点）与逐测试点表
  （分值 / 归属 / 输入 / 答案文件名）；点分数格子展开的详情里也多了子任务得分、
  逐测试点得分与满分、首个不同行，并且这一块改成用 `createElement` 建 DOM
  （原来走 `innerHTML` 拼串，说明里带尖括号就会被当标签）。这一段不依赖提交记录，
  `embedData=false` 时照样在。
- **改**：测试点路径的基准从「题目包根目录」改成「数据目录」（见上面第一条）。
  旧文件里写的 `data/1.in` 加载时归一成 `1.in` 并在下次保存时落成新写法，指向的文件不变。
  面板、Testing 面板、调试里的测试点路径全部改走同一个解析函数，不再各自拼 `rootDir`。

## 0.1.2 — 2026-09-13

三处来自真实使用的反馈（拿它办了一场比赛之后发现的）。

- **修**：新建比赛之后面板里看不到这个比赛。根因是加载器把「一道题都还没有的 contest.json」
  当成配置错误——而刚建出来的比赛正好就是这样，于是整条比赛链路（面板、榜单、以及「新建题目
  顺手加进比赛」）都打不开。现在空的 `problems` 是合法状态，面板会正常显示这场比赛并提示还没有题目。
- **新**：`players/` 下的目录**自动算选手**。只把 `players/<名字>/A.cpp` 放进去，就能出现在榜单与
  「评测全部」里，不用再手改 `contest.json` 的 contestants；那里显式写过的仍然优先（可以自定义
  显示名与目录）。空目录不算选手，自动发现的选手也不会被写回 `contest.json`——`contestants`
  字段因此可以完全不写。
- **修**：侧边栏偶尔一片空白。有两处根因：一是采集状态时抛了异常，面板收不到任何数据，就永远停在
  「正在读取评测配置…」；二是 `contest.json` 读不出来时被静默当成「没有比赛」。现在刷新失败也会
  推一份状态过去，并在面板顶部用横幅写明原因，附「重新读取」与「打开 contest.json」两个按钮。

## 0.1.1 — 2026-09-13

**打包修复**：自带打包器产出的 VSIX 不符合市场后端的格式，上传时报 `TF400898`
（一个不说明原因的内部错误）。0.1.0 的功能没有变，这一版只是把包修对。

- **扩展 ID 改成 `YuChenZhong.verdict-judge`**：市场上 `verdict` 这个名字已被占用。
  扩展内部一律没变——命令、设置还是 `verdict.*`，工作区目录还是 `.verdict/`，
  所以在编辑器里用起来和 0.1.0 完全一样，只是安装时认的 ID 不同。
- `[Content_Types].xml` 按包内实际文件生成：后缀带点、没有后缀的 part 用 `<Override>`
  单独声明。之前那条 `Extension=""` 是非法清单，后端建内容类型表时直接崩。
- 清单补齐市场要的资产与属性：`Content.Details`（README）、`Content.Changelog`、
  `Content.License`、`Icons.Default`，以及 `<Icon>`、仓库链接、Branding、Pricing。
- 包内 README / CHANGELOG / LICENSE 改用市场约定的小写名（`readme.md` / `changelog.md` /
  `LICENSE.txt`），与官方 vsce 的产物一致。

## 0.1.0 — 2026-09-13

**侧边栏评测面板**：活动栏上的 Verdict 图标点开就是操作台，出题、评测、看榜都不用碰 JSON。

- 新增活动栏容器与「评测面板」视图（`verdict.controlPanel`），三个页签：
  - **题目**：比赛信息与题目列表；新建 / 导入 / 导出题目包；选中题目的限制与比较方式
    （`default` / `line` / `real` / `spj` / `interactive`，checker 与 interactor 用文件选择器指定）。
  - **测试点**：按子任务分组，每行给出最近一次判定、用时与内存；`▶` 只跑这一个点、`🐞` 用它调试、
    `⇄` 打开 diff、`▸` 展开看输入 / 标准答案 / 实际输出。子任务可增删、改分值 / 依赖 / 计分方式，
    也能按点均分；测试点可改归属，或移出登记（数据文件保留）。
  - **榜单**：选手 × 题目矩阵，点单元格看重测详情；一键评测全部、导出 HTML、打开完整榜单。
- 面板只是 `problem.json` / `contest.json` 的**视图**：每次编辑都是读-改-写，与手改文件完全等价；
  文件在编辑器外面被改（手改、git 切分支）时面板跟着刷新。
- 单点运行的分数是**下界**：结果标记为 `partial`，面板此时不显示总分，也不会清掉其它测试点的输出。
- 面板里没有网络请求，DOM 全部用 `createElement` / `textContent` 构建（有单测盯着）。

## 0.0.1 — 2026-09-13（首个发布版本）

- **编译与运行**：探测本机的 `g++` / `clang++` / `cl`，编译缓存与首次执行预热；
  `AC` / `WA` / `TLE` / `MLE` / `OLE` / `RE` / `CE` 判定，输出截断与跨平台进程树清理。
- **题目包与子任务**：`problem.json` 描述限制、比较方式、测试点与子任务；扫描 `data/` 登记测试点；
  子任务支持依赖与 `min` / `sum` 计分、部分分。
- **编辑器集成**：Testing 面板、原生 diff（定位首个不同行）、`verdict://` 虚拟文档、CodeLens、
  状态栏与输出通道。
- **比赛**：选手 × 题目整场评测、重测上限、榜单 WebView、自包含（离线可开）的 HTML 成绩单。
- **交互题与 Special Judge**：testlib interactor / checker，含退出码 7 的部分分折算；
  交互题的调试走「录制-重放」。
- **题目包迁移**：ZIP 导入导出；自带打包器产出单一跨平台 VSIX（无原生依赖、无网络请求）。
